<section class="hero">
	<div class="container hero-content animate-in">
		<div class="node-badge">Primary Node Operational</div>
		<h1>Building <span class="accent">Resilient</span> Digital Ecosystems</h1>
		<p class="subtitle">Architecting distributed systems that endure, scale, and evolve beyond monolithic limitations.</p>
		<div class="cta-group">
			<a href="/insights" class="btn primary">Explore Network</a>
			<a href="#about" class="btn secondary">Structural Philosophy</a>
		</div>
	</div>
</section>

<style>
	.hero {
		min-height: 100vh;
		display: flex;
		align-items: center;
		padding-top: 80px;
		position: relative;
	}

	.hero-content {
		max-width: 800px;
	}

	.node-badge {
		display: inline-block;
		font-family: var(--font-mono);
		font-size: 0.75rem;
		color: var(--accent-cyan);
		border: 1px solid var(--accent-cyan);
		padding: 0.25rem 0.75rem;
		border-radius: 20px;
		margin-bottom: 2rem;
		text-transform: uppercase;
		background: rgba(0, 242, 255, 0.05);
	}

	h1 {
		font-size: clamp(3rem, 8vw, 5rem);
		margin-bottom: 1.5rem;
		color: #fff;
	}

	.accent {
		color: var(--accent-cyan);
		text-shadow: 0 0 30px rgba(0, 242, 255, 0.3);
	}

	.subtitle {
		font-size: 1.25rem;
		color: var(--text-secondary);
		margin-bottom: 3rem;
		max-width: 600px;
	}

	.cta-group {
		display: flex;
		gap: 1.5rem;
	}

	.btn {
		padding: 1rem 2rem;
		font-family: var(--font-mono);
		text-transform: uppercase;
		font-size: 0.9rem;
		font-weight: 700;
		border-radius: 4px;
		transition: all 0.3s ease;
	}

	.btn.primary {
		background: var(--accent-cyan);
		color: var(--bg-primary);
	}

	.btn.primary:hover {
		transform: translateY(-2px);
		box-shadow: 0 10px 20px rgba(0, 242, 255, 0.2);
	}

	.btn.secondary {
		border: 1px solid var(--border-color);
		color: #fff;
	}

	.btn.secondary:hover {
		background: rgba(255, 255, 255, 0.05);
		border-color: #fff;
	}
</style>
